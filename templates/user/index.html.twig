{% extends 'user/users.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
	<div class="grix xs4 container">
		<div class="grix xs4 container">
		{% for message in app.flashes('message') %}
			<div class="col-xs4">
				<div class="p-3 my-2 rounded-2 success">{{message}}</div>
			</div>
		{% endfor %}
		<div class="col-xs4">
        
			<h1>Profil de {{ app.user.nom }} </h1>
		</div>
		<div class="col-xs1">Nom :</div>
		<div class="col-xs3">{{ app.user.nom }} </div>
		<div class="col-xs1">Prénom :</div>
		<div class="col-xs3">{{ app.user.prenom }}</div>
		<div class="col-xs1">Email :</div>
		<div class="col-xs3">{{ app.user.email }}</div>
        <div class="col-xs1">Telephone:  </div>
		<div class="col-xs3">{{ app.user.tel }}</div>
        <div class="col-xs1">NumCnam:</div>
		<div class="col-xs3">{{ app.user.NumCnam }}</div>
		<div class="col-xs1">Adresse:</div>
		<div class="col-xs3">{{ app.user.adresse }}</div><br>
		<div class="col-xs4">
            <a href="{{ path ('ModifUser' ) }}" class="btn grey">Modifier le profil</a>
            <br>
            <a href="{{ path ('ModifPassUser' ) }}" class="btn grey">Modifier le mot de passe</a>
            <br>
            <a href="{{ path ('home' ) }}" class="btn grey">retour</a>
           
        </div>

		
        
	</div>
{% endblock %}